apiVersion: batch/v1
kind: Job
metadata:
  name: opening-strength-read-ridge-opening-full
  namespace: bizewu
spec:
  backoffLimit: 0
  ttlSecondsAfterFinished: 3600
  template:
    spec:
      restartPolicy: Never
      imagePullSecrets:
        - name: highfort
      volumes:
        - name: opening-strength-output
          persistentVolumeClaim:
            claimName: bizewu-private-data
      containers:
        - name: reader
          image: registry.corp.highfortfunds.com/bizewu/opening-strength-fit:latest
          imagePullPolicy: Always
          workingDir: /app/opening_strength_fit
          command:
            - python
            - scripts/summarize_opening_results.py
            - --input-dir
            - /mnt/output/opening_strength_fit/ridge_opening_full
          volumeMounts:
            - name: opening-strength-output
              mountPath: /mnt/output
