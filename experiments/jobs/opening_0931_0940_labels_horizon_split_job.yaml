apiVersion: batch/v1
kind: Job
metadata:
  name: os-ds-labelsplit-w0931-0940-v2
  namespace: bizewu
  labels: {app: os-ds-labelsplit-w0931-0940-v2}
spec:
  activeDeadlineSeconds: 86400
  backoffLimit: 2
  completionMode: Indexed
  completions: 7
  parallelism: 2
  ttlSecondsAfterFinished: 86400
  template:
    metadata:
      labels: {app: os-ds-labelsplit-w0931-0940-v2}
    spec:
      restartPolicy: Never
      imagePullSecrets: [{name: highfort}]
      volumes:
        - name: output
          persistentVolumeClaim: {claimName: bizewu-private-data}
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - {key: kubernetes.io/hostname, operator: NotIn, values: [node7, node8]}
      containers:
        - name: splitter
          image: registry.corp.highfortfunds.com/bizewu/opening-strength-fit:20260804-horizon-labels-v3
          imagePullPolicy: Always
          workingDir: /app/opening_strength_fit
          env:
            - name: JOB_COMPLETION_INDEX
              valueFrom:
                fieldRef: {fieldPath: "metadata.annotations['batch.kubernetes.io/job-completion-index']"}
          command: [/bin/bash, -lc]
          args:
            - |
              set -euo pipefail
              YEARS=(2019 2020 2021 2022 2023 2024 2025)
              YEAR="${YEARS[${JOB_COMPLETION_INDEX}]}"
              exec osf-split-horizon-labels --config experiments/runs/opening_0931_0940_labels_horizon_split.toml --year "${YEAR}" --overwrite
          volumeMounts: [{name: output, mountPath: /mnt/output}]
          resources:
            requests: {cpu: "4", memory: 8Gi}
            limits: {cpu: "12", memory: 32Gi}
