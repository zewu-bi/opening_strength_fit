diff --git a/src/opening_strength_fit/commands/k8s_rendering_common.py b/src/opening_strength_fit/commands/k8s_rendering_common.py
index 44510e3..d4d2e13 100644
--- a/src/opening_strength_fit/commands/k8s_rendering_common.py
+++ b/src/opening_strength_fit/commands/k8s_rendering_common.py
@@ -19,12 +19,21 @@ def node_selector_yaml(config: dict, indent: int) -> str:
 
 
 def avoid_nodes_affinity_yaml(config: dict, indent: int) -> str:
-    avoid_nodes = config.get("k8s", {}).get("avoid_nodes", [])
+    k8s = config.get("k8s", {})
+    only_nodes = k8s.get("only_nodes", [])
+    avoid_nodes = k8s.get("avoid_nodes", [])
+    if isinstance(only_nodes, str):
+        only_nodes = only_nodes.replace(",", " ").split()
     if isinstance(avoid_nodes, str):
         avoid_nodes = avoid_nodes.replace(",", " ").split()
-    nodes = [str(node).strip() for node in avoid_nodes if str(node).strip()]
+    included = [str(node).strip() for node in only_nodes if str(node).strip()]
+    excluded = [str(node).strip() for node in avoid_nodes if str(node).strip()]
+    if included and excluded:
+        raise SystemExit("k8s.only_nodes and k8s.avoid_nodes are mutually exclusive")
+    nodes = included or excluded
     if not nodes:
         return ""
+    operator = "In" if included else "NotIn"
 
     lines = [
         f"{' ' * indent}affinity:",
@@ -33,7 +42,7 @@ def avoid_nodes_affinity_yaml(config: dict, indent: int) -> str:
         f"{' ' * (indent + 6)}nodeSelectorTerms:",
         f"{' ' * (indent + 8)}- matchExpressions:",
         f"{' ' * (indent + 10)}- key: kubernetes.io/hostname",
-        f"{' ' * (indent + 12)}operator: NotIn",
+        f"{' ' * (indent + 12)}operator: {operator}",
         f"{' ' * (indent + 12)}values:",
     ]
     lines.extend(f"{' ' * (indent + 14)}- {node}" for node in nodes)
diff --git a/tests/test_k8s_helpers.py b/tests/test_k8s_helpers.py
index 7dd238f..1ce127b 100644
--- a/tests/test_k8s_helpers.py
+++ b/tests/test_k8s_helpers.py
@@ -92,6 +92,35 @@ class K8sHelperTest(unittest.TestCase):
         self.assertIn("TEST_ENDS=(2021-08 2021-09 2021-10)", manifest)
         self.assertNotIn("for MONTH in", manifest)
 
+    def test_sharded_job_can_restrict_gpu_compatibility_nodes(self) -> None:
+        config = {
+            "run": {"id": "temporal_gpu_compatibility", "kind": "temporal_nn"},
+            "window": {
+                "mode": "rolling_monthly",
+                "train_months": 36,
+                "test_months": 6,
+                "test_stride_months": 6,
+                "test_start_month": "2022-01",
+                "test_end_month": "2022-06",
+            },
+            "output": {"layout": "v2"},
+            "k8s": {
+                "only_nodes": ["node10", "node16", "node22"],
+                "resources": {"gpu_limit": "1"},
+            },
+        }
+
+        manifest = render_sharded_training_job(
+            Path("experiments/runs/temporal.toml"),
+            config,
+            "image:tag",
+        )
+
+        self.assertIn("operator: In", manifest)
+        self.assertIn("- node10", manifest)
+        self.assertIn("- node16", manifest)
+        self.assertIn("- node22", manifest)
+
     def test_rolling_halfyear_sharded_job_uses_window_starts(self) -> None:
         config = {
             "run": {
